@model IEnumerable<SchoolProyecto4.Models.cargoModel>

@{
    ViewBag.Title = "ListarPorEnlace";
}

<h2>ListarPorEnlace</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdCargo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreCargo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdCargo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCargo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCargo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCargo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCargo })
        </td>
    </tr>
}

</table>
